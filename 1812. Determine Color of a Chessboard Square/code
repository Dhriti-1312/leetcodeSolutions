class Solution {
public:
    bool squareIsWhite(string coordinates) {
        int x = coordinates[0];
        int y = coordinates[1]; 

        if((x%2 == 1 && y%2 == 0) || (x%2 == 0 && y%2 == 1)){
            return true;
        }
        return false;
    }
};
